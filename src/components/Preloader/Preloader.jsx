import React from "react";
import "./Preloader.css"; // Imports the CSS for the spinner

function Preloader() {
  return <div className="circle-preloader"></div>; // The div that becomes the spinner
}
export default Preloader;
